name: Build and Release WinForms App

on:
  push:
    tags:
      - "v*.*.*"  # Trigger workflow when a new version tag is pushed (e.g., v1.0.0)

jobs:
  build:
    runs-on: windows-latest  # Use Windows runner for building Windows apps

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "8.0.x"  # Use .NET 8.0 SDK

      - name: Restore dependencies
        run: dotnet restore

      - name: Build and Publish Project
        run: |
          dotnet publish -c Release -r win-x64 --no-self-contained true -p:PublishTrimmed=true --output ./publish

      - name: Debug Publish Folder
        run: |
          echo "Listing contents of the publish folder..."
          dir ./publish /s
        shell: powershell


      - name: Verify Published Files
        run: |
          echo "Listing all files in the publish directory:"
          dir ./publish /s
        shell: powershell

      - name: Install WinRAR
        run: |
          choco install winrar -y
        shell: powershell

      - name: Package Entire Publish Folder as RAR
        run: |
          mkdir output
          & "C:\Program Files\WinRAR\WinRAR.exe" a -r -ep1 "output\WinFormsApp.rar" "publish\*"
    
          echo "Checking RAR integrity..."
          & "C:\Program Files\WinRAR\WinRAR.exe" t "output\WinFormsApp.rar"

          echo "Packaging completed successfully."
        shell: powershell


      - name: Verify RAR Contents
        run: |
          & "C:\Program Files\WinRAR\WinRAR.exe" l output/WinFormsApp.rar
        shell: powershell



      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: output/WinFormsApp.rar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
